# NYSC Chatbot — frontend

React + Vite UI. See the [repo root README](../README.md) for setup and how
to run this alongside the backend.

`vite.config.js` proxies `/chat` and `/debug` to `localhost:8000` in dev, and
builds to `../static` (which the backend will eventually serve in production).
